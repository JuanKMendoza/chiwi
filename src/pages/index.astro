---
import Layout from "../layouts/Layout.astro";
import Image from "astro/components/Image.astro";
import Indexmemoriales from "../assets/images/Memorial_personalizado, foto.png";
import Indexrecordatorios from "../assets/images/capibara_cafe.jpg";
import TestimonialsSection from "../components/TestimonialsSection.astro";

import HeroSection from "../components/HeroSection.astro";
import ProductGrid from "../components/ProductGrid.astro";
import TrustSection from "../components/TrustSection.astro";
import FAQSection from "../components/FAQSection.astro";
import NewsletterSection from "../components/NewsletterSection.astro";
import SeasonNavidad   from "../components/seasons/SeasonNavidad.astro";
import SeasonHalloween from "../components/seasons/SeasonHalloween.astro";
import SeasonDiaNinos  from "../components/seasons/SeasonDiaNinos.astro";
import SeasonMadres    from "../components/seasons/SeasonMadres.astro";
import { products } from "../data/products";

const searchQuery = Astro.url.searchParams.get("search");
const searchResults = searchQuery
  ? products.filter(
      (p) =>
        p.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
        p.description.toLowerCase().includes(searchQuery.toLowerCase()) ||
        p.category.some((c) =>
          c.toLowerCase().includes(searchQuery.toLowerCase())
        )
    )
  : [];
---

<Layout
  title="Chiwi Colombia | Velas Kawaii Artesanales Personalizadas - Memoriales para Mascotas"
  description="Velas artesanales kawaii hechas a mano en Colombia. Memoriales únicos para mascotas, regalos personalizados y velas que brillan en la oscuridad. ¡Crea tu vela especial!"
>
  <HeroSection />

  {/* Sección de Confianza — oculta hasta tener datos reales */}

  {searchQuery ? (
    <section id="productos" class="w-full py-16">
      <div class="w-full max-w-7xl mx-auto px-4">
        <div class="mb-8">
          <p class="text-gray-500 text-sm mb-2">Búsqueda activa</p>
          <h2 class="text-3xl font-bold text-gray-900">
            Resultados para: <span class="text-orange-500">"{searchQuery}"</span>
          </h2>
          <a href="/" class="text-sm text-orange-500 hover:underline mt-2 inline-block">← Ver todos los productos</a>
        </div>
        {searchResults.length > 0 ? (
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
            {searchResults.map((product) => (
              <article class="flex flex-col">
                <a href={'/productos/' + product.slug} class="block w-full relative group">
                  <img
                    src={product.image.src}
                    alt={product.alt}
                    class="w-full h-64 object-cover rounded-xl transition-transform group-hover:scale-105"
                    loading="lazy"
                    decoding="async"
                  />
                </a>
                <div class="mt-3 space-y-1">
                  <h3 class="font-bold text-lg">{product.name}</h3>
                  <p class="font-semibold text-gray-800">{"$" + product.price.toLocaleString('es-CO')}</p>
                </div>
                <a
                  href={'/productos/' + product.slug}
                  class="mt-3 w-full flex items-center justify-center gap-2 bg-gradient-to-r from-orange-500 to-pink-500 text-white font-semibold px-4 py-2.5 rounded-full shadow-md hover:scale-105 transition-all"
                >
                  <ion-icon name="eye-outline" class="text-xl" aria-hidden="true" />
                  <span class="text-sm">Ver detalles</span>
                </a>
              </article>
            ))}
          </div>
        ) : (
          <div class="text-center py-16">
            <p class="text-xl text-gray-600 mb-2">
              No encontramos productos para <strong>"{searchQuery}"</strong>.
            </p>
            <p class="text-gray-500 mb-8">¿Hablamos por WhatsApp? Podemos hacer lo que imagines 💫</p>
            <a
              href="https://wa.me/573102278592"
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-3 bg-gradient-to-r from-orange-500 to-pink-500 text-white font-bold px-8 py-4 rounded-full shadow-xl hover:scale-105 transition-all"
            >
              <ion-icon name="logo-whatsapp" class="text-2xl" />
              Hablemos por WhatsApp
            </a>
          </div>
        )}
      </div>
    </section>
  ) : (
    <ProductGrid />
  )}

<!-- ══ SECCIONES DE TEMPORADA (visibilidad controlada por JS según fecha) ══ -->
<div data-season="halloween"  class="season-block" hidden> <SeasonHalloween /> </div>
<div data-season="navidad"    class="season-block" hidden> <SeasonNavidad />   </div>
<div data-season="dia-ninos"  class="season-block" hidden> <SeasonDiaNinos />  </div>
<div data-season="dia-madres" class="season-block" hidden> <SeasonMadres />    </div>


  <TrustSection />

  <!-- Sección Memoriales Mejorada -->
  <section class="relative py-20 overflow-hidden">
    <div
      class="absolute inset-0 bg-gradient-to-br from-pink-50 via-red-50 to-orange-50"
    >
    </div>
    <div
      class="absolute inset-0 opacity-5"
      style="background-image: repeating-linear-gradient(45deg, transparent, transparent 35px, rgba(0,0,0,.05) 35px, rgba(0,0,0,.05) 70px);"
    >
    </div>

    <div
      class="relative w-full max-w-7xl mx-auto px-4 grid grid-cols-1 md:grid-cols-2 gap-16 items-center"
    >
      <div
        class="flex flex-col items-center md:items-start text-center md:text-left space-y-6 order-2 md:order-1"
      >
        <span
          class="inline-block bg-red-100 text-red-600 px-4 py-2 rounded-full text-sm font-semibold"
        >
          Con todo nuestro cariño
        </span>

        <h2 class="text-4xl md:text-5xl font-bold text-gray-900">
          <span class="text-[#FF8F8F]">Memoriales</span> únicos
        </h2>

        <p class="text-xl text-gray-700 leading-relaxed max-w-lg">
          Celebra la vida y el vínculo eterno con tu compañero peludo. Cada
          memorial es una vela única, hecha para honrar su lugar especial en tu
          corazón y mantener vivos los mejores recuerdos.
        </p>

        <div class="space-y-3 w-full">
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#FF8F8F] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">
              Diseño personalizado con la foto de tu mascota
            </p>
          </div>
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#FF8F8F] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">
              Velas que brillan en la oscuridad como su recuerdo
            </p>
          </div>
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#FF8F8F] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">Empaque especial con mensaje dedicado</p>
          </div>
        </div>

        <a
          href="https://wa.me/573102278592?text=Hola!%20Me%20interesa%20crear%20un%20memorial%20para%20mi%20mascota"
          target="_blank"
          rel="noopener noreferrer"
          class="group flex items-center gap-3 bg-[#FF8F8F] text-white font-semibold px-8 py-4 rounded-full shadow-lg hover:scale-105 hover:shadow-xl transition-all duration-300"
        >
          <ion-icon
            name="paw-outline"
            class="text-xl text-white group-hover:animate-bounce"></ion-icon>
          Quiero mi recuerdo especial
          <ion-icon
            name="arrow-forward-outline"
            class="text-lg group-hover:translate-x-1 transition-transform"
          ></ion-icon>
        </a>
      </div>

      <div class="flex justify-center md:justify-end order-1 md:order-2">
        <div class="relative group">
          <div
            class="absolute -inset-4 bg-gradient-to-r from-pink-300 to-orange-300 rounded-2xl blur-xl opacity-30 group-hover:opacity-50 transition-opacity"
          >
          </div>
          <Image
            src={Indexmemoriales}
            alt="Memorial personalizado para mascotas - Vela conmemorativa"
            class="relative rounded-2xl w-full object-cover shadow-2xl"
            width={1000}
            height={800}
            loading="lazy"
            decoding="async"
            format="webp"
            quality={85}
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Sección Recordatorios Mejorada -->
  <section class="relative py-20 overflow-hidden bg-white">
    <div
      class="absolute inset-0 bg-gradient-to-br from-green-50 via-teal-50 to-blue-50 opacity-50"
    >
    </div>

    <div
      class="relative w-full max-w-7xl mx-auto px-4 grid grid-cols-1 md:grid-cols-2 gap-16 items-center"
    >
      <div class="flex justify-center md:justify-start order-1">
        <div class="relative group">
          <div
            class="absolute -inset-4 bg-gradient-to-r from-green-300 to-blue-300 rounded-2xl blur-xl opacity-30 group-hover:opacity-50 transition-opacity"
          >
          </div>
          <Image
            src={Indexrecordatorios}
            alt="Velas personalizadas para celebraciones - Recordatorios únicos"
            class="relative rounded-2xl w-full object-cover shadow-2xl"
            width={1000}
            height={800}
            loading="lazy"
            decoding="async"
            format="webp"
            quality={85}
          />
        </div>
      </div>

      <div
        class="flex flex-col items-center md:items-start text-center md:text-left space-y-6 order-2"
      >
        <span
          class="inline-block bg-green-100 text-green-600 px-4 py-2 rounded-full text-sm font-semibold"
        >
          Para tus celebraciones especiales
        </span>

        <h2 class="text-4xl md:text-5xl font-bold text-gray-900">
          <span class="text-[#A9D3B3]">Recordatorios</span> mágicos
        </h2>

        <p class="text-xl text-gray-700 leading-relaxed max-w-lg">
          Convierte cada celebración en un recuerdo inolvidable. Nuestras velas
          personalizadas llenan de luz y magia cumpleaños, baby showers, bodas y
          mucho más.
        </p>

        <div class="space-y-3 w-full">
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#A9D3B3] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">
              Perfectas para cumpleaños, baby showers y bodas
            </p>
          </div>
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#A9D3B3] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">
              Diseños kawaii que alegran cualquier evento
            </p>
          </div>
          <div class="flex items-start gap-3">
            <div
              class="w-6 h-6 bg-[#A9D3B3] rounded-full flex items-center justify-center flex-shrink-0 mt-1"
            >
              <ion-icon name="checkmark-outline" class="text-white text-sm"
              ></ion-icon>
            </div>
            <p class="text-gray-700">Regalos únicos que todos recordarán</p>
          </div>
        </div>

        <a
          href="https://wa.me/573102278592?text=Hola!%20Me%20interesa%20crear%20recordatorios%20personalizados"
          target="_blank"
          rel="noopener noreferrer"
          class="group flex items-center gap-3 bg-[#A9D3B3] text-white font-semibold px-8 py-4 rounded-full shadow-lg hover:scale-105 hover:shadow-xl transition-all duration-300"
        >
          <ion-icon
            name="gift-outline"
            class="text-xl text-white group-hover:animate-bounce"></ion-icon>
          Crear recuerdos brillantes
          <ion-icon
            name="arrow-forward-outline"
            class="text-lg group-hover:translate-x-1 transition-transform"
          ></ion-icon>
        </a>
      </div>
    </div>
  </section>

  <!-- Sección Testimonios -->
  <TestimonialsSection />

  <NewsletterSection />

  <FAQSection />

  <script>
    (function () {
      // Configuración de temporadas — sincronizada con src/data/seasons.ts
      var seasons = [
        { id: "halloween",  sm: 10, sd: 1,  em: 10, ed: 31 },
        { id: "navidad",    sm: 11, sd: 15, em: 12, ed: 31 },
        { id: "dia-ninos",  sm: 4,  sd: 1,  em: 4,  ed: 30 },
        { id: "dia-madres", sm: 5,  sd: 1,  em: 5,  ed: 31 },
      ];

      var now   = new Date();
      var month = now.getMonth() + 1; // 1-12
      var day   = now.getDate();

      var active = seasons.find(function (s) {
        var afterStart = month > s.sm || (month === s.sm && day >= s.sd);
        var beforeEnd  = month < s.em || (month === s.em && day <= s.ed);
        return afterStart && beforeEnd;
      });

      if (active) {
        var block = document.querySelector('[data-season="' + active.id + '"]');
        if (block) block.removeAttribute("hidden");
      }
    })();
  </script>

</Layout>

<style>
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes gradient {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .animate-fade-in-down {
    animation: fadeInDown 1s ease-out;
  }

  .animate-fade-in-up {
    animation: fadeInUp 1s ease-out;
  }

  .animation-delay-500 {
    animation-delay: 0.5s;
    animation-fill-mode: both;
  }

  .animation-delay-1000 {
    animation-delay: 1s;
    animation-fill-mode: both;
  }

  .animation-delay-1500 {
    animation-delay: 1.5s;
    animation-fill-mode: both;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
    animation-fill-mode: both;
  }

  .animate-gradient {
    background-size: 200% 200%;
    animation: gradient 3s ease infinite;
  }

  .floating-element {
    position: absolute;
    font-size: 2rem;
    animation: float 3s ease-in-out infinite;
    opacity: 0.6;
  }

  /* Fondo de estrellas animadas */
  .stars-background {
    background-image: radial-gradient(2px 2px at 20% 30%, white, transparent),
      radial-gradient(2px 2px at 60% 70%, white, transparent),
      radial-gradient(1px 1px at 50% 50%, white, transparent),
      radial-gradient(1px 1px at 80% 10%, white, transparent),
      radial-gradient(2px 2px at 90% 60%, white, transparent),
      radial-gradient(1px 1px at 33% 80%, white, transparent),
      radial-gradient(1px 1px at 15% 60%, white, transparent);
    background-size: 200% 200%;
    animation: twinkle 5s ease-in-out infinite;
    opacity: 0.6;
  }

  @keyframes twinkle {
    0%,
    100% {
      opacity: 0.3;
    }
    50% {
      opacity: 0.8;
    }
  }

  /* Elementos flotantes de temporada */
  .seasonal-float {
    position: absolute;
    font-size: 3rem;
    animation: float-seasonal 4s ease-in-out infinite;
    opacity: 0.4;
    filter: drop-shadow(0 0 10px rgba(255, 165, 0, 0.5));
  }

  @keyframes float-seasonal {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
    }
    50% {
      transform: translateY(-30px) rotate(10deg);
    }
  }

  /* Pulso sutil */
  @keyframes pulse-subtle {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.05);
    }
  }

  .animate-pulse-subtle {
    animation: pulse-subtle 3s ease-in-out infinite;
  }

  /* Bounce sutil */
  @keyframes bounce-subtle {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }

  .animate-bounce-subtle {
    animation: bounce-subtle 2s ease-in-out infinite;
  }

  /* Gradiente fluido */
  @keyframes gradient-flow {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .animate-gradient-flow {
    background-size: 200% 200%;
    animation: gradient-flow 3s ease infinite;
  }

  /* Efecto de brillo */
  @keyframes glow {
    0%,
    100% {
      opacity: 0.5;
    }
    50% {
      opacity: 1;
    }
  }

  .animate-glow {
    animation: glow 2s ease-in-out infinite;
  }

  /* Tarjeta de temporada */
  .seasonal-card {
    animation: fadeInUp 0.8s ease-out;
    animation-fill-mode: both;
  }

  .seasonal-card:nth-child(1) {
    animation-delay: 0.1s;
  }

  .seasonal-card:nth-child(2) {
    animation-delay: 0.2s;
  }

  .seasonal-card:nth-child(3) {
    animation-delay: 0.3s;
  }
</style>

<script>
  // Counter animation
  const counters = document.querySelectorAll(".counter");
  const observerOptions = {
    threshold: 0.5,
    rootMargin: "0px",
  };

  const observerCallback = (entries: IntersectionObserverEntry[]) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        const counter = entry.target as HTMLElement;
        const target = parseInt(counter.getAttribute("data-target") || "0");
        const duration = 2000;
        const step = target / (duration / 16);
        let current = 0;

        const updateCounter = () => {
          current += step;
          if (current < target) {
            counter.textContent = Math.floor(current).toString();
            requestAnimationFrame(updateCounter);
          } else {
            counter.textContent = target.toString();
          }
        };

        updateCounter();
        observer.unobserve(counter);
      }
    });
  };

  const observer = new IntersectionObserver(observerCallback, observerOptions);
  counters.forEach((counter) => observer.observe(counter));

  // Contador regresivo hasta fin de octubre
  function updateCountdown() {
    const now = new Date();
    const endOfOctober = new Date(now.getFullYear(), 10, 1, 0, 0, 0); // Nov 1, 00:00:00

    if (now.getMonth() !== 9) {
      // Si no es octubre
      // Mostrar "00:00:00"
      document
        .querySelectorAll(
          ".countdown-days, .countdown-hours, .countdown-minutes"
        )
        .forEach((el) => {
          el.textContent = "00";
        });
      return;
    }

    const diff = endOfOctober.getTime() - now.getTime();
    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));

    const daysEl = document.querySelector(".countdown-days");
    const hoursEl = document.querySelector(".countdown-hours");
    const minutesEl = document.querySelector(".countdown-minutes");

    if (daysEl) daysEl.textContent = days.toString().padStart(2, "0");
    if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, "0");
    if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, "0");
  }

  // Actualizar cada minuto
  updateCountdown();
  setInterval(updateCountdown, 60000);
</script>

<style is:global>
  /* Optimizaciones para fluidez del carrusel móvil */
  swiper-container {
    -webkit-overflow-scrolling: touch;
  }
  
  swiper-slide {
    -webkit-transform: translate3d(0, 0, 0);
    transform: translate3d(0, 0, 0);
  }
  
  /* Prevenir scroll bounce en iOS */
  swiper-container::part(wrapper) {
    overscroll-behavior: contain;
  }
</style>